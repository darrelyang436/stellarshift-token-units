# @stellarshift/token-units

Token decimal / unit conversion helpers for EVM interviews and internal tooling.

```bash
npm install github:darrelyang436/stellarshift-token-units
```

```js
const { toWei, fromWei } = require("@stellarshift/token-units");
```